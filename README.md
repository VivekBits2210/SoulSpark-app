# SoulSpark React Native app

Install expo \
`npm install expo-cli --global`

Install dependencies \
`npm install`

Run the app. \
`npx expo start`

Connection refused errors \
`npx expo start --tunnel`

Download the 'Expo Go' app and scan QR code from above command.
